services:
  - type: web
    name: spotify-mp3
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app --workers 1 --threads 8 --timeout 600 --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
